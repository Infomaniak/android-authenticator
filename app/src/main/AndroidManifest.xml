<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAuthenticator">

        <meta-data
            android:name="io.sentry.anr.enable"
            android:value="true" />

        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <meta-data
            android:name="io.sentry.dsn"
            android:value="https://adacdde66d0ca8cca50da9d92847d49f@sentry-mobile.infomaniak.com/23" />

        <activity
            android:name=".MainActivity"
            android:configChanges="colorMode|density|fontScale|fontWeightAdjustment|grammaticalGender|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
            android:exported="true"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- The CrossAppLoginService is designed to be bound, and checks the identity of its clients. -->
        <service
            android:name=".service.CrossAppLoginService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <!-- WARNING: Once this is into a release (even a beta), it shall never be changed. -->
                <action android:name="com.infomaniak.crossapp.login" />
            </intent-filter>
        </service>

    </application>

</manifest>
